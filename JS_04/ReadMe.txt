This is a simple multiplier program done using HTML5, CSS and JavaScript.

This genreates two random numbers (1st no. from 1 to 100 and 2nd no. from 1 to 10)
and multiply them and then check the result with the input given by the user.
If both value match then the score is Increased and if not then vice a versa. 